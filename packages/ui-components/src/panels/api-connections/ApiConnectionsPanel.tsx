import { PanelStub } from '../PanelStub';

export function ApiConnectionsPanel() {
  return (
    <PanelStub
      title="API Connections"
      phase="Phase 5"
      description="Link private (session-bound) and public (marketplace) workspaces. Per-connection release management with version pinning and changelog viewer."
    />
  );
}
