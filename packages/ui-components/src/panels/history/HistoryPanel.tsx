import { PanelStub } from '../PanelStub';

export function HistoryPanel() {
  return (
    <PanelStub
      title="History"
      phase="Phase 6"
      description="Single-request and plan execution history. Stored only in workspace.local — never pushed to Git."
    />
  );
}
