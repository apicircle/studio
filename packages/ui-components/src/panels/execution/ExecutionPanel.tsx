import { PanelStub } from '../PanelStub';

export function ExecutionPanel() {
  return (
    <PanelStub
      title="Execution"
      phase="Phase 6"
      description="Build plans across local and linked collections. Run with or without assertions. Plan-level env priority overrides global priority; context variables stay highest priority."
    />
  );
}
