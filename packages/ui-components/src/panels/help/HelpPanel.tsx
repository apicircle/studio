import { PanelStub } from '../PanelStub';

export function HelpPanel() {
  return (
    <PanelStub
      title="Help Center"
      phase="Phase 7"
      description="Docs, command palette reference, and keyboard shortcuts will land here. No sidebar — full-width content."
    />
  );
}
