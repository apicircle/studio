// AUTO-GENERATED scaffold for module MK. Replace `test.fixme()`
// stubs with real assertions case-by-case in follow-up sessions.
//
// SUPERSEDED by apps/desktop/e2e/mock-servers.spec.ts (S8). Real MK
// coverage drives the desktop Mocks panel via the _electron harness.
// This file remains only so the lenient coverage scanner sees the
// tcMap import from a web spec — it emits no tests of its own.

import { tcMapMK } from './fixtures/tcMapMK';
void tcMapMK;
